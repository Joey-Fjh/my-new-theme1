{% layout none %}

<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {{ content_for_header }}
  </head>

  <body>
    <main>
      <h1>{{ gift_card.balance | money }}</h1>
    </main>
  </body>
</html>
